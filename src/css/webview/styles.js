import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  headerButton: {
    width: 25,
    height: 25,
  },
  headerIcon: { width: '100%', height: '100%' },
});
