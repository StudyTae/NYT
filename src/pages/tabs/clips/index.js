import * as React from 'react';
import { View, Text, Image } from 'react-native';

const ClipsIndex = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>ClipsIndex Screen</Text>
    </View>
  );
};

export default ClipsIndex;
